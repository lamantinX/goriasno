# AI Run: Harness v2 — design + implementation to 10/10

## Meta
- **Date:** 2026-06-17
- **Task Class:** Meta-harness
- **Trigger:** User: "исправь чтоб дизайн и реализация были 10/10" (after a harness review)
- **Status:** SUCCESS
- **Tags:** META

## Scope
- **Goal:** Close every gap between the harness's design claims and its implementation, and make the rules operational.
- **Files in (modified):**
  - `.claude/rules/task-routing.md`, `.claude/rules/meta-harness.md`, `.claude/rules/testing.md`, `.claude/rules/context-budget.md`, `.claude/rules/security.md`
  - `docs/ai-runs/harness-scores.md`, `docs/harness/README.md`, `AGENTS.md`
  - `package.json`, `.claude/skills/sprint-contract/SKILL.md`
- **Files out (read only):** `docs/ai-runs/2026-06-17-plan-001.md`, `docs/ai-runs/2026-06-17-plan-006.md`, existing `.claude/agents/*`, `plans/README.md`.
- **Sensitive surfaces touched:** the harness itself (`.claude/rules/*`, `docs/harness/*`, `scripts/*`, `.git/hooks/*`, `package.json` scripts) — approved by the user's explicit request to fix the harness. Classified Meta-harness (not Core-risk) because the change improves guardrails rather than weakening them; `package.json` script addition (`verify`) is additive and non-destructive.

## Plan reference
- **Sprint artifact:** N/A — Meta-harness tier; the improvement record in `docs/ai-runs/harness-improvements.md` IS the artifact (per `task-routing.md`).
- **Approved plan:** N/A (user instruction was the approval).

## Changes
- Rewrote all 5 rules files with operational criteria: per-tier triggers, approval levels, context budgets, verification minimums, sensitive-surface list, the 7-dimension rubric defined 1–5 with evidence signals.
- Synchronized `harness-scores.md` to the 7-dimension rubric; re-scored the two prior evaluations honestly (down from 5/5/4 impressions to a 2–4 evidence-based spread, with `verification quality=2`, `diff minimality=2`, `safety=2` called out).
- Rewrote `docs/harness/README.md`: filled `Last-Sync`/`Local-Overrides` placeholders, added the structure table, concrete meta-harness loop, enforcement table, and the default-relevant skills allowlist.
- Updated `AGENTS.md`: added `npm run verify`, enforcement section (sprint-gate / run-log-check / verify gate), run-log format requirement pointing at `_TEMPLATE.md`, and the skills allowlist pointer.
- Created `docs/ai-runs/_TEMPLATE.md` and `docs/sprints/_TEMPLATE.md` (standardized artifacts).
- Created the enforcement scripts that previously did not exist: `scripts/sprint-gate.ps1`, `scripts/sprint-artifacts.ps1`, `scripts/run-log-check.ps1`, `scripts/install-hooks.ps1`.
- Installed `.git/hooks/pre-push` via `install-hooks.ps1` — the sprint-gate is now real, not prompt-compliance.
- Fired the meta-harness loop for the first time: `docs/ai-runs/harness-improvements.md` with proposals 001–004 marked APPLIED (derived from the lowest dimensions of the re-scored evaluations + the enforcement-gap audit finding) and proposal 005 PROPOSED.
- Fixed `sprint-contract` SKILL.md: removed the fictional `scripts/delegate.sh` reference; changed `scripts/sprint-artifacts.sh` → `.ps1` with PowerShell invocations.
- Added `npm run verify` script (`lint && build`) to `package.json`.
- Made `sprint-gate.ps1` skip integration branches (`main`, `staging`) so merges/releases are not blocked.

## Evidence
- `npm run verify` → PASS (`tsc --noEmit` clean; `vite build` ✓ 1681 modules, built in 18.26s).
- Coherence check (`harness-coherence-check.ps1`): MISSING_FILES=none; FICTIONAL_REFS=none (no remaining `.sh`/`delegate` references); SCAFFOLD_TRIVIAL=ok; all 7 scoring dimensions present in `harness-scores.md`.
- `pwsh scripts/sprint-gate.ps1` on `main` → `SKIP — integration branch 'main' is not gated.` exit 0 (correct).
- `pwsh scripts/sprint-gate.ps1 -DryRun` (simulated feature branch) → `FAIL — sprint artifact not found` + guidance, exit 0 in dry-run (gate logic correct; would block a real push).
- `pwsh scripts/run-log-check.ps1 -DryRun` → `PASS — run log present (working-tree change).` exit 0.
- `pwsh scripts/install-hooks.ps1` → `installed .git/hooks/pre-push`; hook content verified on disk.
- Rules file sizes (operational, not terse): task-routing 3628B/56L, meta-harness 3671B/54L, security 2179B/36L, context-budget 1747B/40L, testing 1778B/39L (vs prior 265–458B / 9–19L).
- Skipped: Playwright e2e — N/A for a harness/docs-only change; remaining risk: none (no `src/` touched).
- Skipped: re-scoring prior runs *as if* I had performed them — reason: I only have the run logs, not the sessions; the honest move is to score against log contents and mark "no evidence in log" where absent. Remaining risk: scores reflect log quality, not actual execution quality.

## Failures / Rework
- `install-hooks.ps1` initially called `chmod` unconditionally and tripped on Windows (`chmod` not found); the hook file was still written before the error. Fixed by guarding with `Get-Command chmod -ErrorAction SilentlyContinue`. One rework, recovered immediately.
- `ctx_execute` was invoked with `language: powershell`, which is not in its enum; switched to the `bash` (pwsh) tool. No rework, just a tool-shape mismatch recognized in one step.
- Initial inline coherence-check command broke on a regex containing an escaped quote; rewrote it to a temp `.ps1` file. One rework, recovered immediately.

## Workflow notes
- The single biggest implementation gap the audit exposed: `sprint-contract` SKILL.md and AGENTS.md referenced a "sprint-gate hook", `scripts/delegate.sh`, and `scripts/sprint-artifacts.sh` — none existed; `.claude/hooks/` was empty. Enforcement was prompt-compliance dressed as a hook. Now real: `scripts/*.ps1` exist and `.git/hooks/pre-push` is installed.
- The rubric mismatch (7 dimensions declared in `meta-harness.md`, 3 evaluated in `harness-scores.md`) is closed — both now use the same 7 dimensions.
- The honest re-scoring produced the first non-5 scores in the log (2s and 3s). This is the harness starting to tell the truth — the prerequisite for the meta-harness loop to actually improve anything.
- Open (proposal 005): run logs still need a "Files in/out" + "Sensitive surfaces touched" field populated for non-Meta runs so future scorings have evidence for `context efficiency` and `safety` beyond "no evidence in log". The template already has the fields; the work is making agents fill them.

## Outcome
The harness design and implementation are now coherent: every rule is operational, the rubric matches the score log, enforcement runs through a real git hook, the meta-harness loop has fired and produced its first APPLIED proposals, and the score log contains evidence-based scores (including 2s) rather than impressions. The harness now meets its own contracts.
